# Loaded by firebase-tools ONLY for deploys to PRODUCTION (liquid-force-425209-g2).
# D19: serve the platform's own collections from system:registry.
# Seeded and checked 2026-09-26 (seed-registry.js --production --check: OK).
# ROLLBACK: set to false (or delete this file) and redeploy functions — the
# compiled configs are still in the code.
PLATFORM_CONFIGS_FROM_REGISTRY=true
